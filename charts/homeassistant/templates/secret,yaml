{{- if .Values.github.ssh_key -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "chart.basename" . }}-ssh
  labels: {{ include "chart.labels" . | nindent 4 }}
data:
  privatekey: {{ .Values.github.ssh_key }}
type: kubernetes.io/ssh-auth
{{- end -}}